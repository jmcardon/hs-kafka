
==================== Tidy Core ====================

Result size of Tidy Core
  = {terms: 1,407, types: 959, coercions: 0, joins: 6/28}

$trModule4 = "main"#

$trModule3 = TrNameS $trModule4

$trModule2 = "Kafka.Internal.RecordBatch"#

$trModule1 = TrNameS $trModule2

$trModule = Module $trModule3 $trModule1

Rec {
$wpokeUvarint
  = \ ww ww1 eta ->
      case <# ww1 128# of {
        __DEFAULT ->
          case writeWord8OffAddr#
                 ww 0# (wordToWord8# (int2Word# (orI# (andI# ww1 127#) 128#))) eta
          of s2
          { __DEFAULT ->
          $wpokeUvarint (plusAddr# ww 1#) (uncheckedIShiftRA# ww1 7#) s2
          };
        1# ->
          case writeWord8OffAddr# ww 0# (wordToWord8# (int2Word# ww1)) eta
          of s2
          { __DEFAULT ->
          (# s2, plusAddr# ww 1# #)
          }
      }
end Rec }

Rec {
$wwriteAllRecords
  = \ ww ds ww1 eta ->
      case ds of {
        [] -> eta;
        : payload rest ->
          case payload of { BS ipv ipv1 ipv2 ->
          join {
            $j y
              = join {
                  $j1 y1
                    = let { bodySize = +# 4# (+# ipv2 (+# y1 y)) } in
                      join {
                        $j2 ww2
                          = case $wpokeUvarint ww ww2 eta of { (# ww3, ww4 #) ->
                            case writeWord8OffAddr# ww4 0# 0#Word8 ww3 of s2 { __DEFAULT ->
                            case writeWord8OffAddr# (plusAddr# ww4 1#) 0# 0#Word8 s2 of s1
                            { __DEFAULT ->
                            join {
                              $j3 ww5
                                = case $wpokeUvarint (plusAddr# ww4 2#) ww5 s1 of
                                  { (# ww6, ww7 #) ->
                                  case writeWord8OffAddr# ww7 0# 1#Word8 ww6 of s3 { __DEFAULT ->
                                  case >=# ipv2 0# of {
                                    __DEFAULT ->
                                      case $wpokeUvarint
                                             (plusAddr# ww7 1#)
                                             (xorI# (uncheckedIShiftL# ipv2 1#) -1#)
                                             s3
                                      of
                                      { (# ww8, ww9 #) ->
                                      case keepAlive#
                                             ipv1
                                             ww8
                                             (\ s ->
                                                (# copyAddrToAddrNonOverlapping# ipv ww9 ipv2 s,
                                                   () #))
                                      of
                                      { (# ipv3, ipv4 #) ->
                                      let { p4 = plusAddr# ww9 ipv2 } in
                                      case writeWord8OffAddr# p4 0# 0#Word8 ipv3 of s4
                                      { __DEFAULT ->
                                      $wwriteAllRecords (plusAddr# p4 1#) rest (+# ww1 1#) s4
                                      }
                                      }
                                      };
                                    1# ->
                                      case $wpokeUvarint
                                             (plusAddr# ww7 1#) (uncheckedIShiftL# ipv2 1#) s3
                                      of
                                      { (# ww8, ww9 #) ->
                                      case keepAlive#
                                             ipv1
                                             ww8
                                             (\ s ->
                                                (# copyAddrToAddrNonOverlapping# ipv ww9 ipv2 s,
                                                   () #))
                                      of
                                      { (# ipv3, ipv4 #) ->
                                      let { p4 = plusAddr# ww9 ipv2 } in
                                      case writeWord8OffAddr# p4 0# 0#Word8 ipv3 of s4
                                      { __DEFAULT ->
                                      $wwriteAllRecords (plusAddr# p4 1#) rest (+# ww1 1#) s4
                                      }
                                      }
                                      }
                                  }
                                  }
                                  } } in
                            case >=# ww1 0# of {
                              __DEFAULT -> jump $j3 (xorI# (uncheckedIShiftL# ww1 1#) -1#);
                              1# -> jump $j3 (uncheckedIShiftL# ww1 1#)
                            }
                            }
                            }
                            } } in
                      case >=# bodySize 0# of {
                        __DEFAULT -> jump $j2 (xorI# (uncheckedIShiftL# bodySize 1#) -1#);
                        1# -> jump $j2 (uncheckedIShiftL# bodySize 1#)
                      } } in
                case >=# ipv2 0# of {
                  __DEFAULT ->
                    let { x = xorI# (uncheckedIShiftL# ipv2 1#) -1# } in
                    case <# x 128# of {
                      __DEFAULT ->
                        case <# x 16384# of {
                          __DEFAULT ->
                            case <# x 2097152# of {
                              __DEFAULT ->
                                case <# x 268435456# of {
                                  __DEFAULT -> jump $j1 5#;
                                  1# -> jump $j1 4#
                                };
                              1# -> jump $j1 3#
                            };
                          1# -> jump $j1 2#
                        };
                      1# -> jump $j1 1#
                    };
                  1# ->
                    let { x = uncheckedIShiftL# ipv2 1# } in
                    case <# x 128# of {
                      __DEFAULT ->
                        case <# x 16384# of {
                          __DEFAULT ->
                            case <# x 2097152# of {
                              __DEFAULT ->
                                case <# x 268435456# of {
                                  __DEFAULT -> jump $j1 5#;
                                  1# -> jump $j1 4#
                                };
                              1# -> jump $j1 3#
                            };
                          1# -> jump $j1 2#
                        };
                      1# -> jump $j1 1#
                    }
                } } in
          case >=# ww1 0# of {
            __DEFAULT ->
              let { x = xorI# (uncheckedIShiftL# ww1 1#) -1# } in
              case <# x 128# of {
                __DEFAULT ->
                  case <# x 16384# of {
                    __DEFAULT ->
                      case <# x 2097152# of {
                        __DEFAULT ->
                          case <# x 268435456# of {
                            __DEFAULT -> jump $j 5#;
                            1# -> jump $j 4#
                          };
                        1# -> jump $j 3#
                      };
                    1# -> jump $j 2#
                  };
                1# -> jump $j 1#
              };
            1# ->
              let { x = uncheckedIShiftL# ww1 1# } in
              case <# x 128# of {
                __DEFAULT ->
                  case <# x 16384# of {
                    __DEFAULT ->
                      case <# x 2097152# of {
                        __DEFAULT ->
                          case <# x 268435456# of {
                            __DEFAULT -> jump $j 5#;
                            1# -> jump $j 4#
                          };
                        1# -> jump $j 3#
                      };
                    1# -> jump $j 2#
                  };
                1# -> jump $j 1#
              }
          }
          }
      }
end Rec }

buildRecordBatch1
  = \ ds ds1 ds2 eta ->
      case ds of { Ptr ww ->
      case ds2 of { I# ww1 ->
      case $wwriteAllRecords ww ds1 ww1 eta of ww2 { __DEFAULT ->
      (# ww2, () #)
      }
      }
      }

Rec {
$wgo
  = \ ww ww1 ds ->
      case ds of {
        [] -> ww;
        : payload rest ->
          case payload of { BS bx bx1 bx2 ->
          join {
            $j y
              = join {
                  $j1 y1
                    = let { bodySize = +# 4# (+# bx2 (+# y1 y)) } in
                      case >=# bodySize 0# of {
                        __DEFAULT ->
                          let { x = xorI# (uncheckedIShiftL# bodySize 1#) -1# } in
                          case <# x 128# of {
                            __DEFAULT ->
                              case <# x 16384# of {
                                __DEFAULT ->
                                  case <# x 2097152# of {
                                    __DEFAULT ->
                                      case <# x 268435456# of {
                                        __DEFAULT -> $wgo (+# 5# (+# bodySize ww)) (+# ww1 1#) rest;
                                        1# -> $wgo (+# 4# (+# bodySize ww)) (+# ww1 1#) rest
                                      };
                                    1# -> $wgo (+# 3# (+# bodySize ww)) (+# ww1 1#) rest
                                  };
                                1# -> $wgo (+# 2# (+# bodySize ww)) (+# ww1 1#) rest
                              };
                            1# -> $wgo (+# 1# (+# bodySize ww)) (+# ww1 1#) rest
                          };
                        1# ->
                          let { x = uncheckedIShiftL# bodySize 1# } in
                          case <# x 128# of {
                            __DEFAULT ->
                              case <# x 16384# of {
                                __DEFAULT ->
                                  case <# x 2097152# of {
                                    __DEFAULT ->
                                      case <# x 268435456# of {
                                        __DEFAULT -> $wgo (+# 5# (+# bodySize ww)) (+# ww1 1#) rest;
                                        1# -> $wgo (+# 4# (+# bodySize ww)) (+# ww1 1#) rest
                                      };
                                    1# -> $wgo (+# 3# (+# bodySize ww)) (+# ww1 1#) rest
                                  };
                                1# -> $wgo (+# 2# (+# bodySize ww)) (+# ww1 1#) rest
                              };
                            1# -> $wgo (+# 1# (+# bodySize ww)) (+# ww1 1#) rest
                          }
                      } } in
                case >=# bx2 0# of {
                  __DEFAULT ->
                    let { x = xorI# (uncheckedIShiftL# bx2 1#) -1# } in
                    case <# x 128# of {
                      __DEFAULT ->
                        case <# x 16384# of {
                          __DEFAULT ->
                            case <# x 2097152# of {
                              __DEFAULT ->
                                case <# x 268435456# of {
                                  __DEFAULT -> jump $j1 5#;
                                  1# -> jump $j1 4#
                                };
                              1# -> jump $j1 3#
                            };
                          1# -> jump $j1 2#
                        };
                      1# -> jump $j1 1#
                    };
                  1# ->
                    let { x = uncheckedIShiftL# bx2 1# } in
                    case <# x 128# of {
                      __DEFAULT ->
                        case <# x 16384# of {
                          __DEFAULT ->
                            case <# x 2097152# of {
                              __DEFAULT ->
                                case <# x 268435456# of {
                                  __DEFAULT -> jump $j1 5#;
                                  1# -> jump $j1 4#
                                };
                              1# -> jump $j1 3#
                            };
                          1# -> jump $j1 2#
                        };
                      1# -> jump $j1 1#
                    }
                } } in
          case >=# ww1 0# of {
            __DEFAULT ->
              let { x = xorI# (uncheckedIShiftL# ww1 1#) -1# } in
              case <# x 128# of {
                __DEFAULT ->
                  case <# x 16384# of {
                    __DEFAULT ->
                      case <# x 2097152# of {
                        __DEFAULT ->
                          case <# x 268435456# of {
                            __DEFAULT -> jump $j 5#;
                            1# -> jump $j 4#
                          };
                        1# -> jump $j 3#
                      };
                    1# -> jump $j 2#
                  };
                1# -> jump $j 1#
              };
            1# ->
              let { x = uncheckedIShiftL# ww1 1# } in
              case <# x 128# of {
                __DEFAULT ->
                  case <# x 16384# of {
                    __DEFAULT ->
                      case <# x 2097152# of {
                        __DEFAULT ->
                          case <# x 268435456# of {
                            __DEFAULT -> jump $j 5#;
                            1# -> jump $j 4#
                          };
                        1# -> jump $j 3#
                      };
                    1# -> jump $j 2#
                  };
                1# -> jump $j 1#
              }
          }
          }
      }
end Rec }

$wbuildRecords
  = \ payloads ->
      case $wgo 0# 0# payloads of ww { __DEFAULT ->
      runRW#
        (\ s ->
           case <# ww 0# of {
             __DEFAULT ->
               case newPinnedByteArray# ww s of { (# ipv, ipv1 #) ->
               let { ipv2 = mutableByteArrayContents# ipv1 } in
               case keepAlive#
                      (PlainPtr ipv1)
                      ipv
                      (\ eta ->
                         case $wwriteAllRecords ipv2 payloads 0# eta of ww1 { __DEFAULT ->
                         (# ww1, () #)
                         })
               of
               { (# ipv4, ipv5 #) ->
               case lazy runRW# (\ _ -> (# ipv4, ipv2 #)) of { (# ipv6, ipv7 #) ->
               (# ipv7, ipv1, ww #)
               }
               }
               };
             1# -> case mallocPlainForeignPtrBytes2 of {}
           })
      }

buildRecords
  = \ payloads ->
      case $wbuildRecords payloads of { (# ww, ww1, ww2 #) ->
      BS ww (PlainPtr ww1) ww2
      }

$wwrapRecordBatch
  = \ ww ww1 ww2 ww3 ww4 ww5 ww6 ww7 ->
      runRW#
        (\ s ->
           let { totalSize = +# 61# ww7 } in
           case <# totalSize 0# of {
             __DEFAULT ->
               case newPinnedByteArray# totalSize s of { (# ipv, ipv1 #) ->
               let { ipv2 = mutableByteArrayContents# ipv1 } in
               case keepAlive#
                      (PlainPtr ipv1)
                      ipv
                      (\ s1 ->
                         case keepAlive#
                                ww6
                                s1
                                (\ s2 ->
                                   (# copyAddrToAddrNonOverlapping# ww5 (plusAddr# ipv2 61#) ww7 s2,
                                      () #))
                         of
                         { (# ipv4, ipv5 #) ->
                         let { p = plusAddr# ipv2 21# } in
                         case writeWord16OffAddr#
                                p
                                0#
                                (wordToWord16# (byteSwap16# (word16ToWord# (int16ToWord16# ww3))))
                                ipv4
                         of s2
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 2#)
                                0#
                                (wordToWord32#
                                   (byteSwap32#
                                      (word32ToWord# (int32ToWord32# (intToInt32# (-# ww4 1#))))))
                                s2
                         of s3
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 6#) 0# (byteSwap64# 0#Word64) s3
                         of s4
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 14#) 0# (byteSwap64# 0#Word64) s4
                         of s5
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 22#) 0# (byteSwap64# (int64ToWord64# ww)) s5
                         of s6
                         { __DEFAULT ->
                         case writeWord16OffAddr#
                                (plusAddr# p 30#)
                                0#
                                (wordToWord16# (byteSwap16# (word16ToWord# (int16ToWord16# ww1))))
                                s6
                         of s7
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 32#)
                                0#
                                (wordToWord32# (byteSwap32# (word32ToWord# (int32ToWord32# ww2))))
                                s7
                         of s8
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 36#)
                                0#
                                (wordToWord32#
                                   (byteSwap32# (word32ToWord# (int32ToWord32# (intToInt32# ww4)))))
                                s8
                         of s9
                         { __DEFAULT ->
                         runRW#
                           (\ s10 ->
                              case {__ffi_static_ccall_unsafe main:crc32c_extend :: Word32#
                                                 -> Addr#
                                                 -> Word64#
                                                 -> State# RealWorld
                                                 -> (# State# RealWorld, Word32# #)}
                                     0#Word32 p (int64ToWord64# (intToInt64# (+# 40# ww7))) s10
                              of
                              { (# ds2, ds3 #) ->
                              case lazy (W32# ds3) of { W32# ipv6 ->
                              case writeWord64OffAddr# ipv2 0# (byteSwap64# 0#Word64) s9 of s11
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv2 8#)
                                     0#
                                     (wordToWord32#
                                        (byteSwap32#
                                           (word32ToWord#
                                              (int32ToWord32# (intToInt32# (+# 49# ww7))))))
                                     s11
                              of s12
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv2 12#) 0# (wordToWord32# (byteSwap32# 0##)) s12
                              of s13
                              { __DEFAULT ->
                              case writeWord8OffAddr# (plusAddr# ipv2 16#) 0# 2#Word8 s13 of s14
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv2 17#)
                                     0#
                                     (wordToWord32# (byteSwap32# (word32ToWord# ipv6)))
                                     s14
                              of s15
                              { __DEFAULT ->
                              (# s15, () #)
                              }
                              }
                              }
                              }
                              }
                              }
                              })
                         }
                         }
                         }
                         }
                         }
                         }
                         }
                         }
                         })
               of
               { (# ipv4, ipv5 #) ->
               case lazy runRW# (\ _ -> (# ipv4, ipv2 #)) of { (# ipv6, ipv7 #) ->
               (# ipv7, ipv1, totalSize #)
               }
               }
               };
             1# -> case mallocPlainForeignPtrBytes2 of {}
           })

wrapRecordBatch
  = \ pid epoch baseSeq comprAttr recordCount records ->
      case pid of { I64# ww ->
      case epoch of { I16# ww1 ->
      case baseSeq of { I32# ww2 ->
      case comprAttr of { I16# ww3 ->
      case recordCount of { I# ww4 ->
      case records of { BS ww5 ww6 ww7 ->
      case $wwrapRecordBatch ww ww1 ww2 ww3 ww4 ww5 ww6 ww7 of
      { (# ww8, ww9, ww10 #) ->
      BS ww8 (PlainPtr ww9) ww10
      }
      }
      }
      }
      }
      }
      }

buildRecordBatch
  = \ pid epoch baseSeq comprAttr payloads ->
      case pid of { I64# ipv ->
      case epoch of { I16# ipv1 ->
      case baseSeq of { I32# ipv2 ->
      case comprAttr of { I16# ipv3 ->
      case $wgo 0# 0# payloads of ww { __DEFAULT ->
      runRW#
        (\ s ->
           let { totalSize = +# 61# ww } in
           case <# totalSize 0# of {
             __DEFAULT ->
               case newPinnedByteArray# totalSize s of { (# ipv4, ipv5 #) ->
               let { ipv6 = mutableByteArrayContents# ipv5 } in
               let { ipv7 = PlainPtr ipv5 } in
               case keepAlive#
                      ipv7
                      ipv4
                      (\ s1 ->
                         case $wwriteAllRecords (plusAddr# ipv6 61#) payloads 0# s1 of ww1
                         { __DEFAULT ->
                         case $wlenAcc payloads 0# of ww2 { __DEFAULT ->
                         let { p = plusAddr# ipv6 21# } in
                         case writeWord16OffAddr#
                                p
                                0#
                                (wordToWord16# (byteSwap16# (word16ToWord# (int16ToWord16# ipv3))))
                                ww1
                         of s2
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 2#)
                                0#
                                (wordToWord32#
                                   (byteSwap32#
                                      (word32ToWord# (int32ToWord32# (intToInt32# (-# ww2 1#))))))
                                s2
                         of s3
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 6#) 0# (byteSwap64# 0#Word64) s3
                         of s4
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 14#) 0# (byteSwap64# 0#Word64) s4
                         of s5
                         { __DEFAULT ->
                         case writeWord64OffAddr#
                                (plusAddr# p 22#) 0# (byteSwap64# (int64ToWord64# ipv)) s5
                         of s6
                         { __DEFAULT ->
                         case writeWord16OffAddr#
                                (plusAddr# p 30#)
                                0#
                                (wordToWord16# (byteSwap16# (word16ToWord# (int16ToWord16# ipv1))))
                                s6
                         of s7
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 32#)
                                0#
                                (wordToWord32# (byteSwap32# (word32ToWord# (int32ToWord32# ipv2))))
                                s7
                         of s8
                         { __DEFAULT ->
                         case writeWord32OffAddr#
                                (plusAddr# p 36#)
                                0#
                                (wordToWord32#
                                   (byteSwap32# (word32ToWord# (int32ToWord32# (intToInt32# ww2)))))
                                s8
                         of s9
                         { __DEFAULT ->
                         runRW#
                           (\ s10 ->
                              case {__ffi_static_ccall_unsafe main:crc32c_extend :: Word32#
                                                 -> Addr#
                                                 -> Word64#
                                                 -> State# RealWorld
                                                 -> (# State# RealWorld, Word32# #)}
                                     0#Word32 p (int64ToWord64# (intToInt64# (+# 40# ww))) s10
                              of
                              { (# ds1, ds2 #) ->
                              case lazy (W32# ds2) of { W32# ipv8 ->
                              case writeWord64OffAddr# ipv6 0# (byteSwap64# 0#Word64) s9 of s11
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv6 8#)
                                     0#
                                     (wordToWord32#
                                        (byteSwap32#
                                           (word32ToWord#
                                              (int32ToWord32# (intToInt32# (+# 49# ww))))))
                                     s11
                              of s12
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv6 12#) 0# (wordToWord32# (byteSwap32# 0##)) s12
                              of s13
                              { __DEFAULT ->
                              case writeWord8OffAddr# (plusAddr# ipv6 16#) 0# 2#Word8 s13 of s14
                              { __DEFAULT ->
                              case writeWord32OffAddr#
                                     (plusAddr# ipv6 17#)
                                     0#
                                     (wordToWord32# (byteSwap32# (word32ToWord# ipv8)))
                                     s14
                              of s15
                              { __DEFAULT ->
                              (# s15, () #)
                              }
                              }
                              }
                              }
                              }
                              }
                              })
                         }
                         }
                         }
                         }
                         }
                         }
                         }
                         }
                         }
                         })
               of
               { (# ipv8, ipv9 #) ->
               case lazy runRW# (\ _ -> (# ipv8, ipv6 #)) of
               { (# ipv10, ipv11 #) ->
               BS ipv11 ipv7 totalSize
               }
               }
               };
             1# -> case mallocPlainForeignPtrBytes2 of {}
           })
      }
      }
      }
      }
      }


